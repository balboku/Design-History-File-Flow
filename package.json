{
  "name": "design-history-file-flow",
  "private": true,
  "scripts": {
    "prisma:generate": "prisma generate",
    "prisma:push": "prisma db push",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@prisma/client": "^6.19.2",
    "prisma": "^6.19.2"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "vitest": "^4.1.2"
  }
}
